# محول كتب الشاملة - Shamela Books Converter

![الإصدار](https://img.shields.io/badge/الإصدار-2.0.0-blue)
![الحالة](https://img.shields.io/badge/الحالة-نشط-green)
![المنصة](https://img.shields.io/badge/المنصة-Windows-lightgrey)

## 📖 نظرة عامة

محول كتب الشاملة هو تطبيق شامل وقوي لتحويل كتب مكتبة الشاملة من قواعد بيانات Microsoft Access (.accdb, .mdb, .bok) إلى قواعد بيانات MySQL الحديثة. يتميز التطبيق بواجهة مستخدم سهلة الاستخدام ونظام تحويل متقدم يحافظ على سلامة البيانات والنصوص العربية.

## ✨ المميزات الرئيسية

### 🔄 تحويل متقدم
- **دعم شامل للتنسيقات**: .accdb, .mdb, .bok
- **تحويل تلقائي لملفات .bok**: نظام ذكي لتحويل ملفات .bok إلى Access قابل للقراءة
- **حفظ التشكيل العربي**: نظام متطور للحفاظ على التشكيل والحركات العربية
- **تنسيق HTML**: تحويل النصوص إلى تنسيق HTML مع وسوم مناسبة

### 🗄️ إدارة قواعد البيانات
- **نظام فهرسة محسن**: فهرسة داخلية (`internal_index`) للصفحات
- **ربط ذكي للفصول**: ربط الفصول بالصفحات باستخدام النظام الجديد
- **دعم متعدد المجلدات**: إدارة الكتب متعددة المجلدات
- **تحسين الأداء**: فهارس محسنة لسرعة البحث والاستعلام

### 🖥️ واجهة المستخدم
- **تصميم عصري**: واجهة مستخدم جذابة وسهلة الاستخدام
- **تقدم مرئي**: شريط تقدم مفصل مع معلومات الحالة
- **سجل شامل**: نظام سجل متقدم مع فلترة حسب نوع الرسالة
- **تقارير الجلسة**: تقارير مفصلة لكل جلسة تحويل

### 📊 الإحصائيات والتقارير
- **إحصائيات فورية**: معلومات مفصلة عن عدد الصفحات والفصول
- **تقارير HTML**: إمكانية حفظ التقارير بتنسيق HTML
- **تقييم الأداء**: معلومات عن وقت التحويل والسرعة
- **تتبع الأخطاء**: رصد وتسجيل شامل للأخطاء

## 🚀 التثبيت والتشغيل

### متطلبات النظام
- **نظام التشغيل**: Windows 10 أو أحدث
- **Python**: 3.8 أو أحدث (للتطوير)
- **Microsoft Access Database Engine**: مطلوب لقراءة ملفات Access
- **اتصال إنترنت**: للاتصال بخادم MySQL

### التثبيت السريع (للمستخدمين)
1. حمل ملف `ShamielaConverter.exe` من مجلد `dist`
2. شغل الملف مباشرة - لا يحتاج تثبيت!
3. تأكد من وجود Microsoft Access Database Engine

### التثبيت للمطورين

```bash
# استنساخ المشروع
git clone [repository-url]
cd ShamielaBookConverter

# تثبيت المتطلبات
pip install -r requirements.txt

# تشغيل التطبيق
python main.py
```

### بناء ملف EXE

```bash
# تثبيت PyInstaller
pip install pyinstaller

# بناء الملف التنفيذي
python build_exe.py
```

## 📋 دليل الاستخدام

### 1️⃣ تحديد الملفات
- اضغط على "اختيار ملفات الكتب"
- حدد ملفات الكتب (.accdb, .mdb, .bok)
- يمكن اختيار عدة ملفات مرة واحدة

### 2️⃣ إعداد قاعدة البيانات
- أدخل معلومات خادم MySQL:
  - **الخادم**: عنوان IP أو اسم الخادم
  - **المنفذ**: عادة 3306
  - **قاعدة البيانات**: اسم قاعدة البيانات
  - **المستخدم وكلمة المرور**: بيانات الدخول
- اضغط "اختبار الاتصال" للتأكد
- احفظ الإعدادات للاستخدام المستقبلي

### 3️⃣ بدء التحويل
- اضغط "بدء التحويل"
- راقب التقدم من خلال شريط التقدم والسجل
- يمكن إيقاف العملية في أي وقت

### 4️⃣ مراجعة النتائج
- اطلع على تقرير الجلسة المفصل
- احفظ السجل والتقارير للمرجعية
- تحقق من قاعدة البيانات للتأكد من نجاح التحويل

## 🏗️ بنية المشروع

```
ShamielaBookConverter/
├── main.py                     # نقطة الدخول الرئيسية
├── src/                        # كود المصدر
│   ├── shamela_converter.py    # محرك التحويل الأساسي
│   ├── shamela_gui.py          # واجهة المستخدم الرسومية
│   └── simple_bok_support.py   # دعم ملفات .bok
├── resources/                  # الموارد والأصول
│   ├── icons/                  # الأيقونات
│   ├── images/                 # الصور
│   └── data/                   # البيانات والإعدادات
├── build_exe.py               # سكريبت بناء EXE
├── requirements.txt           # المتطلبات
├── setup.py                   # إعداد التوزيع
└── docs/                      # التوثيق
```

## 🔧 التكوين المتقدم

### إعدادات قاعدة البيانات
يمكن تخصيص إعدادات قاعدة البيانات من خلال ملف `resources/data/config.json`:

```json
{
  "database_settings": {
    "default_host": "your-server.com",
    "default_port": 3306,
    "charset": "utf8mb4",
    "use_unicode": true
  }
}
```

### تخصيص الواجهة
```json
{
  "ui_settings": {
    "window_width": 900,
    "window_height": 700,
    "theme": "clam",
    "font_family": "Arial"
  }
}
```

## 🗃️ هيكل قاعدة البيانات

التطبيق ينشئ الجداول التالية:

- **`authors`**: معلومات المؤلفين
- **`publishers`**: معلومات الناشرين  
- **`books`**: معلومات الكتب
- **`pages`**: صفحات الكتب مع النظام المحسن
- **`chapters`**: فصول الكتب مع الربط المتقدم
- **`book_indexes`**: فهارس المصطلحات
- **`conversion_logs`**: سجلات عمليات التحويل

### المميزات الجديدة في النسخة 2.0
- **`internal_index`**: فهرسة داخلية فريدة للصفحات
- **ربط متقدم للفصول**: استخدام `internal_index` بدلاً من `page_number`
- **دعم النص الكامل**: فهارس FULLTEXT للبحث السريع
- **إجراءات مخزونة**: وظائف جاهزة للإحصائيات والبحث

## 🐛 إصلاح المشاكل

### مشاكل شائعة وحلولها

**1. خطأ في الاتصال بقاعدة Access**
```
خطأ: Microsoft Access Driver غير موجود
```
**الحل**: ثبت Microsoft Access Database Engine من موقع مايكروسوفت

**2. فشل في الاتصال بـ MySQL**
```
خطأ: Can't connect to MySQL server
```
**الحل**: 
- تحقق من صحة معلومات الخادم
- تأكد من أن المنفذ 3306 مفتوح
- تحقق من صحة اسم المستخدم وكلمة المرور

**3. مشاكل في ترميز النص**
```
خطأ: UnicodeDecodeError
```
**الحل**: التطبيق يدعم UTF-8 تلقائياً، تأكد من أن قاعدة البيانات تستخدم `utf8mb4`

**4. ملفات .bok لا تعمل**
```
خطأ: لا يمكن قراءة ملف .bok
```
**الحل**: التطبيق يحول ملفات .bok تلقائياً، تأكد من أن الملف سليم وغير تالف

## 📈 الأداء والتحسين

### نصائح لتحسين الأداء
- **استخدم اتصال سريع**: اتصال إنترنت سريع يحسن سرعة التحويل
- **ذاكرة كافية**: تأكد من وجود ذاكرة كافية للملفات الكبيرة
- **قم بالتحويل على دفعات**: للملفات الكثيرة، قسمها على دفعات
- **راقب السجل**: استخدم السجل لتتبع الأداء وحل المشاكل

### معايير الأداء
- **الملفات الصغيرة** (< 10 MB): 1-2 دقيقة
- **الملفات المتوسطة** (10-100 MB): 5-15 دقيقة  
- **الملفات الكبيرة** (> 100 MB): 15-60 دقيقة

*الأوقات تعتمد على سرعة الجهاز والشبكة*

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:

1. Fork المشروع
2. إنشاء branch للميزة الجديدة
3. Commit التغييرات
4. Push إلى Branch
5. فتح Pull Request

## 📞 الدعم والمساعدة

### طرق الحصول على المساعدة
- **GitHub Issues**: لإبلاغ الأخطاء وطلب المميزات
- **الوثائق**: راجع ملفات التوثيق في مجلد `docs/`
- **السجل**: استخدم نظام السجل المدمج لتشخيص المشاكل

### معلومات المطور
- **المطور**: GitHub Copilot AI Assistant
- **السنة**: 2025
- **الترخيص**: MIT License

## 📝 رخصة الاستخدام

هذا المشروع مرخص تحت رخصة MIT - راجع ملف `LICENSE` للتفاصيل.

## 🔄 سجل التغييرات

### الإصدار 2.0.0 (2025)
- ✨ نظام فهرسة داخلية جديد (`internal_index`)
- 🔧 تحسين ربط الفصول بالصفحات
- 🎨 واجهة مستخدم محدثة وأكثر جاذبية
- 📊 نظام تقارير وإحصائيات متقدم
- 🐛 إصلاح العديد من الأخطاء
- 🚀 تحسين الأداء والسرعة
- 📱 دعم أفضل لملفات .bok

### الإصدار 1.0.0
- 🎉 الإصدار الأول
- ⚡ تحويل أساسي من Access إلى MySQL
- 🖥️ واجهة مستخدم بسيطة
- 📋 دعم ملفات .accdb و .mdb

---

**شكراً لاستخدام محول كتب الشاملة! 📚✨**